#ifndef KEYBOARD_SCAN_ASCII_PAIR_H
#define KEYBOARD_SCAN_ASCII_PAIR_H

typedef struct keyboard_scan_ascii_pair
{
    unsigned char scancode;
    unsigned char ascii;
} keyboard_scan_ascii_pair;

#endif