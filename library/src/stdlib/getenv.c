#include "../stdlib.h"

char* getenv(const char* name)
{
    // Since our OS doesn't suppore environment variables yet, just pass one simple NULL.
    // NOTE: In future put here call to system API.
    return NULL;
}