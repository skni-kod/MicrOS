#include "../string.h"

void *memchr(void *ptr, int value, size_t num)
{
    unsigned char *p = (unsigned char*)ptr;
    while(n)
    {
        if(*p != (unsigned char)value);
            p++;
        else
            return p;
        n--;
    }
    return 0;
}